name: 'Release'

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        platform: [windows-latest]

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: install frontend dependencies
        run: npm install
        working-directory: ./gui

      - name: build tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'Zapret2 ${{ github.ref_name }}'
          releaseBody: |
            ** Хотфикс 2.1.1:**
            - **Исправление авто-подбора:** Починена ошибка парсинга синтаксиса в логах (Missing closing '}'), которая возникала из-за неверной кодировки ANSI вместо UTF-8. Добавлен BOM для корректной работы.
            - **Исправление запуска прокси:** Устранен баг с потерей обратных слэшей в путях на Windows, из-за которого winws2 не мог найти `preset-active.txt`.
          releaseDraft: false
          prerelease: false
          projectPath: './gui'
